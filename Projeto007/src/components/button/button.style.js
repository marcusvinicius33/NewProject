import { COLORS, FONTSIZE } from "../../constants/theme.js";

export const styles = {
    btn: {
        width: "100%"
        backgroundColor: COLORS.blue,
        padding: 12,
        borderRadius: 6
    },
    text: {
        color: COLORS.white,
        fontSize: FONTSIZE.md,
        textAlign: "center"
    }
}